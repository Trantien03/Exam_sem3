@model List<Exam_sem3.Entities.Department>
@{

}
<div class="container mt-4">
    <h1 class="mb-3">Departments</h1>
    <a class="btn btn-primary mb-3" asp-controller="Department" asp-action="Create">Create a Department</a>

    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Code</th>
                <th>Location</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Code</td>
                    <td>@item.Location</td>
                    <td>
                        <a class="btn btn-warning btn-sm" asp-controller="Department" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        <a class="btn btn-danger btn-sm" onclick="return confirm('Delete department @item.Name')" asp-controller="Department" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>